const LOGO = { link: '/', label: 'IOTABOTS' }
const HOME = { link: '/', label: 'Start' }

// Hides anything except the LOGO (MENU[0]) - see Navigation.tsx
export const MENU = [LOGO]

// Only visible for mobile screens
export const MOBILE_MENU = [HOME]